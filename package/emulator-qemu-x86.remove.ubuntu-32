#!/bin/sh

TIZEN_SDK_INSTALL_PATH=`echo $INSTALLED_PATH`
if [ -z $TIZEN_SDK_INSTALL_PATH ]
then
#   echo "There is no TIZEN_SDK_PATH ENV" >> /tmp/emulator.log
   exit 2;
fi

gksudo update-rc.d tizen-kvm remove
gksudo rm -f /etc/init.d/tizen-kvm
gksudo rm -f /lib/udev/rules.d/45-tizen-kvm.rules
